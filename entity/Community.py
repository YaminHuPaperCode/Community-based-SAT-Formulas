# the structure of a community
class Community:
    def __init__(self, index):
        # the index of this community
        self.index = index
        # the variables that this community contains
        self.variableSet = set()
